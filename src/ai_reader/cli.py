"""CLI entry point (stub — implemented by subagent C)."""

import sys


def main() -> int:
    print("ai-reader: stub — implementation pending (subagent C)", file=sys.stderr)
    return 1


if __name__ == "__main__":
    sys.exit(main())
